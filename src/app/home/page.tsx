import { Layout } from '@/components/components/layout';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Home | Cine Mercy',
};

export default function Home() {
  return <Layout>HI</Layout>;
}
